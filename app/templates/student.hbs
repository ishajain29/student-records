<h1> List Of Students </h1>
<h4>{{#link-to 'student.courses'}}{{model.student.name}}{{/link-to}}</h4>
<ul>
  {{#each model.student.courses as |object|}}
    <li>{{object.name}}
    <ol>
  		{{#each object.subjects as |obj|}}
    		<li>{{obj.name}} {{obj.aptitude}}</li>
  		{{/each}}
	</ol>
	
	</li>
  {{/each}}

</ul>
{{outlet}}
